#include "LeetList.h"
#include <iostream>

class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        return nullptr;
    }
};

int main() {
    // 1,2,3,4,5
    List ent(5);
    ent.insert(4);
    ent.insert(3);
    ent.insert(2);
    ListNode* head = ent.insert(1);
}